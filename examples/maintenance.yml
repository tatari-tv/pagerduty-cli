# Maintenance window definition for `pd maintenance create --from-file`.
# Required: start-time, end-time, services (list of service names/IDs).

# Optional human-readable description shown in the PagerDuty UI.
description: Platform DB migration - expected blackout

# ISO-8601 timestamps. Both required.
start-time: 2026-05-01T02:00:00Z
end-time: 2026-05-01T04:00:00Z

# Services the window covers. ID, slug, or display name; tiered match.
services:
  - Platform API
